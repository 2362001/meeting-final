spring:
  application:
    name: basic-java

server:
  port: ${SERVER_PORT:6080}
  ssl:
    enabled: false

# LiveKit configuration
livekit:
  api:
    key: ${LIVEKIT_API_KEY:devkey}
    secret: ${LIVEKIT_API_SECRET:devsecret_at_least_32_characters_long}
